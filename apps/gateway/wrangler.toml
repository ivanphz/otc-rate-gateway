name = "otc-gateway-api"
main = "src/worker.js"
compatibility_date = "2026-06-10"

# 关键：不加这行，每次 wrangler deploy 都会把 CF 后台手动加的 Variables 全部清空
# （Wrangler 默认把 wrangler.toml 当作变量的唯一真相源，凡是没写在这里的变量都会被删除）。
# 加上它之后，后台手动配置的 NODES 等变量不会再被部署冲掉。
keep_vars = true

# —— 💰 成本价功能的 KV 绑定（一次性配置）——
# 1. 创建命名空间：CF 后台 > 存储和数据库 > KV > 创建，名字随意（如 otc-data）
# 2. 复制它的「命名空间 ID」，填到下面 id，去掉三行注释，推送部署
# ⚠️ KV 绑定必须写在本文件里！keep_vars 只保环境变量，保不住后台手动加的绑定——
#    后台加的 KV 绑定会在下次 CI 部署时被 Wrangler 清掉（和你之前 NODES 被冲掉是同类坑）。
#    命名空间 ID 不是敏感信息（没有 API Token 拿到 ID 也无法访问数据），可放心进公开仓库。
 [[kv_namespaces]]
 binding = "OTC_KV"
 id = "2d42c92215854c01804f24ed108438ef"

# ⚠️ 私人节点地址不要写在这里！在 CF 后台 Settings > Variables > 添加 NODES(明文变量即可)，
#    或本地执行： npx wrangler secret put NODES
#    NODES 值示例(JSON 字符串)：
#    [{"id":"🇯🇵 VPS-Tokyo","url":"https://your-vps.example.com"},{"id":"☁️ CF Worker","url":"https://your-spider.example.com"}]
